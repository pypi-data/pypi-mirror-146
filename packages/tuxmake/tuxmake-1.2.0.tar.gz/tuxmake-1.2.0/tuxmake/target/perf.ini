[target]
description = "perf programs and library"
commands = {make} -C tools/perf
    && {make} -C tools/perf install-bin
    && {tar_caf} {build_dir}/perf.tar.xz -C {build_dir}/perf-install .

[makevars]
DESTDIR = {build_dir}/perf-install/usr

[artifacts]
perf.tar.xz = perf.tar.xz
