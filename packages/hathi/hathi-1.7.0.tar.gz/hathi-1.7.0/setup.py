#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['hathi']

package_data = \
{'': ['*']}

install_requires = \
['asyncpg', 'rich']

extras_require = \
{'mssql': ['pymssql==2.1.5'], 'mysql': ['mysqlclient']}

entry_points = \
{'console_scripts': ['hathi = hathi.__main__:main']}

setup(name='hathi',
      version='1.7.0',
      description='A SQL host scanner and dictionary attack tool. Comes with a script (`filter_pass.py`) to filter a series of password lists based on password strength.',
      author='Anthony Shaw',
      author_email=None,
      url=None,
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
     )
