[tool.black]
line-length = 95
target-version = ["py35"]

[tool.isort]
profile = "black"
py_version = "35"

[tool.coverage.run]
parallel = false
branch = true
source = ["edc_glucose"]

[tool.coverage.paths]
source = ["edc_glucose", ".tox/*/site-packages"]

[tool.coverage.report]
show_missing = true
skip_covered = true
omit = ["requirements.txt"]
